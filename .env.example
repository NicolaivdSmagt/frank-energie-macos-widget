# Copy to .env and fill in your own 10-character Apple Developer Team ID.
# Find it in Xcode > Settings > Accounts (select your team), then run:
#   set -a; source .env; set +a; xcodegen generate
DEVELOPMENT_TEAM=XXXXXXXXXX
